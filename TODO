Globbing
 Handle ~ and $HOME correctly.

Option parsing
 Handle command line switches. At least config, help, info, version
 should be supported.

Standard configuration file
 The program should look on some standard path for a configuration
 file. A different file should be specified via a command line switch
 (--config=file).

Documentation
 Add man pages and comment the code.
